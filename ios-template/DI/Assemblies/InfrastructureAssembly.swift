import Foundation
import Swinject

struct InfrastructureAssembly: ServiceAssembly {
    func assemble(container: Container) {
        // Minimal template infrastructure registrations.
        // Add concrete cross-cutting services here when introduced.
    }
}
